apiVersion: traefik.containo.us/v1alpha1
kind: TraefikService
metadata:
  name: pod-info-service
  namespace: test
spec:
  weighted:
    services:
      - name: podinfo # k8s service name that you need to create for stable application version
        port: 9898
      - name: podinfo-canary # k8s service name that you need to create for new application version
        port: 9898
